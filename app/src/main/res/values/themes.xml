<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NetzunAvanzado" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NetzunAvanzado" parent="Base.Theme.NetzunAvanzado" />

    <style name="Theme.NetzunAvanzado.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="chipsEntry" parent="Widget.MaterialComponents.Chip.Entry">
        <item name="chipBackgroundColor">#A42EB3</item>
        <item name="chipStrokeColor">#858484</item>
        <item name="chipCornerRadius">8dp</item>
        <item name="android:textColor">@drawable/chip_textcolor</item>
    </style>

    <style name="Theme.NetzunAvanzado.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >

    </style>

    <style name="Theme.NetzunAvanzado.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >

    </style>
</resources>